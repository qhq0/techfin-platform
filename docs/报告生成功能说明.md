# 报告生成功能说明

> **功能入口**：要素提取 completed=true 后，前端下载 Word 报告文档
>
> **前置条件**：已完成要素提取（`GET /sxd/extract-status/{task_id}` 返回 `completed: true`）
>
> **关联文档**：[`要素提取功能说明.md`](要素提取功能说明.md)

---

## 一、核心业务逻辑

### 1.1 背景

要素提取完成后，系统已从上传的财务报表中提取了结构化的财务数据。报告生成功能从这些提取数据中聚合财务报表各期间的资产负债表关键科目和利润表关键科目，同时查询**客户基本信息**（`sxd_profile` 表），通过 **Apache POI** 组件向 Word 模板文档的占位符位置插入数据，生成 `.docx` 格式的报告文件下载。

### 1.2 生成报告接口

```
GET /techfin/sxd/report/{task_id}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `task_id`（路径变量） | String | 是 | 提交资料返回的任务 ID，格式 `TASK-<32位hex>` |

**成功响应：** 返回 `.docx` 文件下载（`Content-Type: application/vnd.openxmlformats-officedocument.wordprocessingml.document`），文件名 `企业信息报告.docx`。

### 1.3 Word 报告内容

生成的 Word 文档包含以下三个部分：

#### 一、企业基本信息

从 `sxd_profile` 表查询的 34 个字段，通过替换模板中的 `{{字段名}}` 占位符插入：

| 占位符 | 字段说明 | 对应表字段 |
|--------|---------|-----------|
| `{{cst_nm}}` | 客户名称 | `cst_nm` |
| `{{credit_code}}` | 统一社会信用代码 | `credit_code` |
| `{{fd_dt}}` | 成立时间 | `fd_dt` |
| `{{lgl_rprs_nm}}` | 法定代表人 | `lgl_rprs_nm` |
| `{{act_cntlr_nm}}` | 实控人姓名 | `act_cntlr_nm` |
| `{{rgst_cpamt}}` | 注册资本 | `rgst_cpamt` |
| `{{arcptl_cpamt}}` | 实收资本 | `arcptl_cpamt` |
| `{{cpct_tpcd}}` | 企业类型 | `CPCT_TPCD` |
| `{{entp_sz_cd}}` | 企业规模 | `entp_sz_cd` |
| `{{entp_bliy}}` | 所属行业 | `entp_bliy` |
| `{{dtl_adr}}` | 注册地址 | `dtl_adr` |
| `{{org_oprt_scop_dsc}}` | 经营范围 | `org_oprt_scop_dsc` |
| `{{tech_tag}}` | 企业科技资质类型 | `tech_tag` |
| `{{tech_flow}}` | 技术流评价结果 | `tech_flow` |
| `{{kc_score}}` | 科创分评价结果 | `kc_score` |
| `{{entp_ptnt_num}}` | 企业专利数量 | `ENTP_PTNT_NUM` |
| `{{entp_prct_new_tp_ptnt_num}}` | 实用新型专利数 | `ENTPPRCTNEWTPPTNT_NUM` |
| `{{entp_ivt_ptnt_num}}` | 发明专利数 | `ENTP_IVT_PTNT_NUM` |
| `{{clst_5yr_inn_rs_wcopr_num}}` | 软件著作权数量 | `CLST5YRINNRSWCOPR_NUM` |
| `{{if_loan}}` | 是否我行贷款客户 | `if_loan` |
| `{{product_name}}` | 最新贷款品种 | `product_name` |
| `{{loan_amount}}` | 贷款金额 | `loan_amount` |
| `{{loan_term}}` | 贷款期限 | `loan_term` |
| `{{loan_balance}}` | 贷款当前余额 | `loan_balance` |
| `{{dep_bal}}` | 对公存款余额 | `dep_bal` |
| `{{dep_bal_dt}}` | 对公存款余额日期 | `dep_bal_dt` |
| `{{dep_aadbal}}` | 对公存款年日均余额 | `dep_aadbal` |
| `{{acc_start_dt}}` | 开立账户时间 | `acc_start_dt` |
| `{{acc_type}}` | 对公账户类型 | `acc_type` |
| `{{isug_pnum}}` | 代发人数 | `isug_pnum` |
| `{{avg_12_isug_amt}}` | 月均代发工资 | `avg_12_isug_amt` |
| `{{if_yuqi}}` | 近两年存在贷款逾期 | `if_yuqi` |
| `{{ltgtrltd_ind}}` | 存在司法纠纷 | `ltgtrltd_ind` |
| `{{if_rad_alarm}}` | 存在RAD红色预警 | `if_rad_alarm` |

#### 二、资产负债表关键科目

| 项目 | 2024年 | 2023年 | ... | 2024年增长率 | ... |
|------|--------|--------|-----|------------|-----|
| 资产总计 | 53,846.51 | 27,510.45 | ... | 95.73% | ... |
| ... | ... | ... | ... | ... | ... |
| 资产负债率 | 96.36% | 97.73% | ... | -2.08% | ... |

#### 三、利润表关键科目

| 项目 | 2024年 | 2023年 | ... | 2024年增长率 | ... |
|------|--------|--------|-----|------------|-----|
| 营业收入 | 34,439.14 | 17,317.23 | ... | 98.87% | ... |
| ... | ... | ... | ... | ... | ... |
| 毛利润率 | 12.46% | 4.32% | ... | 188.52% | ... |
| 净利润率 | 3.88% | -13.58% | ... | -128.57% | ... |
| 研发费用占比 | 3.42% | 8.12% | ... | -57.89% | ... |

#### 表格生成规则

| 规则 | 说明 |
|------|------|
| 列标题 | 根据每个报表文档的 `report_date` 自动生成：12月31日 → `YYYY年`，其他日期 → `YYYY年M月` |
| 金额格式 | 原始数据单位为**元**，表中统一转为**万元**（÷10000），千分位保留2位小数 |
| 增长率列 | 所有增长率列统一放在表格最后，对每对相邻年末（12-31）列计算同比。计算同比：`(当期-上期)/\|上期\|×100%`，上期为0时显示 `-` |
| 资产负债率 | 资产负债表最后一行计算值：`负债合计 / 资产总计 × 100%`，单独计算增长率 |
| 毛利润率 | 利润表计算行：`(营业收入-营业成本) / 营业收入 × 100%` |
| 净利润率 | 利润表计算行：`净利润 / 营业收入 × 100%` |
| 研发费用占比 | 利润表计算行：`研发费用 / 营业收入 × 100%` |

---

### 1.4 判断逻辑

#### 确定使用的资产负债表类型

对每个财务报表文档，通过以下步骤确定使用哪个表查询数据：

```
┌─ 查询 docTableExtractState/{docId}
│
├─ 是否有报表附注信息（dib_intervening_y_auditreport_jh）?
│   ├─ 是 → 查询"财务报表口径"（item_value）
│   │   ├─ "1"(单一) → 使用母公司资产负债表（dib_fin_balance_parent）
│   │   └─ "2"(合并) → 使用合并资产负债表（dib_fin_balance）
│   │
│   └─ 否 → 直接比较资产负债表提取状态
│       ├─ 仅合并表为Y → 使用合并表（dib_fin_balance）
│       ├─ 仅母公司表为Y → 使用母公司表（dib_fin_balance_parent）
│       └─ 均为Y或均非Y → 默认使用合并表（dib_fin_balance）
│
└─ → 调用 queryData（{docId, tableName}）查询科目数据
```

#### 数据聚合规则

1. 每个文档对应一个 `report_date`，查询到的 `current_amount` 即为该日期的科目值
2. 多个文档（不同 `report_date`）的数据按科目名称 `item` 合并，形成完整的多日期表格
3. 表格列按 `report_date` 的先后顺序排列
4. 未匹配到数据的科目显示 `-`
5. 查询失败或返回空数组时仍输出对应日期列，该列所有项目显示 `-`

### 1.5 利润表关键科目

#### 利润表科目匹配规则

利润表科目来源与资产负债表相同的外部 `queryData` 接口，按 `item_standard` 精确匹配确定科目名称：

| 显示名称 | 匹配的 `item_standard` 值 |
|---------|------------------------|
| 营业收入 | `营业总收入` |
| 营业成本 | `营业成本` |
| 管理费用 | `管理费用` |
| 销售费用 | `销售费用` |
| 财务费用 | `财务费用` |
| 研发费用 | `研发费用` |
| 营业利润 | `营业利润` |
| 利润总额 | `利润总额` |
| 净利润 | `净利润` |

> 匹配方式为精确匹配：数据记录的 `item_standard` 字段必须与上表所列值完全一致。营业收入在 API 中标准名称为"营业总收入"，而非"营业收入"。

#### 确定使用的利润表类型

对每个财务报表文档，通过以下步骤确定使用哪个利润表查询数据：

```
┌─ 查询 docTableExtractState/{docId}
│
├─ 是否有报表附注信息（dib_intervening_y_auditreport_jh）?
│   ├─ 是 → 查询"财务报表口径"（item_value）
│   │   ├─ "1"(单一) → 使用母公司利润表（dib_fin_profit_statement_parent）
│   │   └─ "2"(合并) → 使用合并利润表（dib_fin_profit_statement）
│   │
│   └─ 否 → 直接比较利润表提取状态
│       ├─ 仅合并表为Y → 使用合并表（dib_fin_profit_statement）
│       ├─ 仅母公司表为Y → 使用母公司表（dib_fin_profit_statement_parent）
│       └─ 均为Y或均非Y → 默认使用合并表（dib_fin_profit_statement）
│
└─ → 调用 queryData（{docId, tableName}）查询科目数据
```

> 财务报表口径的判断逻辑与资产负债表完全一致（共享 `dib_intervening_y_auditreport_jh` 表口径），同一个文档的资产负债表和利润表会选择同一口径（单一→母公司表，合并→合并表）。

---

## 二、处理流程

```
┌─ 前端点击"生成报告"
│
├─ 后端处理流程：
│   ① 根据 taskId 查 application_record → 获取 customer_no
│   ② 查 sxd_profile → 获取客户基本信息
│   ③ 根据 taskId 查 application_doc → 获取财务报表 docId 列表
│   ④ 对每个 docId：
│      ├─ 资产负债表：docTableExtractState → 确定合并/母公司表 → queryData
│      └─ 利润表：docTableExtractState → 确定合并/母公司表 → queryData
│   ⑤ 按科目和日期聚合数据
│   ⑥ 加载 Word 模板文件 → 替换 {{占位符}} → 插入数据表格
│   ⑦ 删除 application_doc 中该 task_id 的文档记录
│   ⑧ 更新 application_record.status = COMPLETED
│   ⑨ 返回 .docx 文件字节流
│
└─ 前端下载"企业信息报告.docx"
```

### 模板替换机制

报告基于 Word 模板（`.docx`）生成，模板中的占位符格式为 `{{占位符名称}}`：

- **客户信息字段**：`{{cst_nm}}`、`{{credit_code}}` 等 34 个占位符 → 替换为字段值
- **资产负债表表格**：`{{资产负债表关键科目}}` → 在该位置插入 XWPFTable
- **利润表表格**：`{{利润表关键科目}}` → 在该位置插入 XWPFTable

代码通过 `XmlCursor` 在占位符段落位置插入表格，然后移除占位符段落。用户可直接替换模板文件，只要保留上述占位符即可。

---

## 三、模板文件

| 项目 | 说明 |
|------|------|
| 文件位置 | `techfin-controller/src/main/resources/templates/sxd/报告模板.docx` |
| 配置项 | `api.report-template-path=classpath:templates/sxd/报告模板.docx` |
| 加载方式 | Spring `ResourceLoader`，支持 `classpath:` 前缀和外部文件路径 |
| 占位符 | `{{字段名}}`（34个客户字段）、`{{资产负债表关键科目}}`、`{{利润表关键科目}}` |

> **更换模板**：替换该文件即可，新模板必须包含所有占位符。外部路径可通过 `api.report-template-path` 配置覆盖（如 `file:/opt/app/templates/报告模板.docx`）。

---

## 四、外部 API 调用

### 表格提取状态 `GET /api/extract/open/doc/docTableExtractState/{doc_id}`

```
Headers:
  c1-token: <从 application.properties 配置读取>

Response 200:
{
  "data": [
    {
      "tableName": "dib_intervening_y_auditreport_jh",
      "tableCnName": "报表附注信息",
      "extractState": "Y",
      "auditState": "Y",
      "pageNum": "24"
    },
    {
      "tableName": "dib_fin_balance",
      "tableCnName": "合并资产负债表",
      "extractState": "Y",
      "auditState": "Y",
      "pageNum": "24,25,26"
    },
    {
      "tableName": "dib_fin_balance_parent",
      "tableCnName": "母公司资产负债表",
      "extractState": "Y",
      "auditState": "Y",
      "pageNum": "26,27,28"
    }
  ],
  "success": true,
  "code": "1",
  "message": "操作成功"
}
```

### 提取数据查询（资产负债表）`POST /api/extract/open/doc/queryData`

同[要素提取功能说明 — 提取数据查询](要素提取功能说明.md#提取数据查询-post-apiextractopendocquerydata)，`tableName` 为 `dib_fin_balance` 或 `dib_fin_balance_parent`。

Response data 数组元素：

```json
{
  "item_code": "B1100101BB",
  "item": "货币资金",
  "current_amount": 12345678901.23,
  "last_amount": 98765432109.87,
  "report_date": "2025-06-30",
  "unit": "元"
}
```

### 提取数据查询（利润表）`POST /api/extract/open/doc/queryData`

`tableName` 为 `dib_fin_profit_statement` 或 `dib_fin_profit_statement_parent`。

Response data 数组元素结构与资产负债表相同：

```json
{
  "item_code": "P1000001PT",
  "item": "一、营业总收入",
  "item_standard": "营业总收入",
  "current_amount": 91093762553.97,
  "last_amount": 83451164646.53,
  "report_date": "2025-06-30",
  "unit": "元"
}
```

> 利润表与资产负债表共享同一个 `queryData` 接口，仅 `tableName` 参数不同。响应结构完全一致，后端复用 `BalanceSheetRecord` DTO 解析，并通过 `item_standard` 字段进行科目匹配。

### 提取数据查询（报表附注）`POST /api/extract/open/doc/queryData`

`tableName` 为 `dib_intervening_y_auditreport_jh`，用于查询"财务报表口径"。

Response data 数组元素：

```json
{
  "item": "财务报表口径",
  "item_value": "2",
  "report_date": "2025-06-30"
}
```

| 字段 | 说明 |
|------|------|
| `item` | 项目名称，`财务报表口径` 或 `财务报表截止日期` |
| `item_value` | `1`=单一（母公司表），`2`=合并（合并表） |

### 响应解析对照

| 接口 | 解析方式 | DTO |
|------|---------|-----|
| `docTableExtractState` | `getDataAsList(DocTableStateRecord.class)` | `DocTableStateRecord` (tableName, extractState) |
| `queryData`（资产负债表） | `getDataAsList(BalanceSheetRecord.class)` | `BalanceSheetRecord` (item, itemStandard, currentAmount, reportDate) |
| `queryData`（利润表） | `getDataAsList(BalanceSheetRecord.class)` | `BalanceSheetRecord` (item, itemStandard, currentAmount, reportDate) |
| `queryData`（报表附注） | `getDataAsList(AuditReportItem.class)` | `AuditReportItem` (item, itemValue) |

---

## 五、数据模型

### application_doc 表（文档明细表）

| 字段 | 类型 | 说明 |
|------|------|------|
| `doc_id` | VARCHAR(64) (PK) | 资料批量新增返回的文档 ID |
| `task_id` | VARCHAR(64) | 关联 `application_record.task_id` |
| `business_type` | VARCHAR(32) | 业务类型（docTypeId），finance=1273757631042949120 |
| `report_date` | VARCHAR(10) | 财报报告日期，作为表格列标题来源 |

### 关键 DTO

| 类 | 位置 | 说明 |
|----|------|------|
| `DocTableStateRecord` | `model/.../dto/external/` | 表格提取状态响应 data 元素 |
| `BalanceSheetRecord` | `model/.../dto/external/` | 资产负债表/利润表 queryData 响应 data 元素（item, itemStandard, currentAmount） |
| `AuditReportItem` | `model/.../dto/external/` | 审计报告附注 queryData 响应 data 元素（item+itemValue） |
| `CustomerProfile` | `model/.../entity/` | `sxd_profile` 映射实体（34 个字段） |

### 关键科目列表

#### 资产负债表关键科目

| # | 科目名称 | 数据来源 |
|---|---------|---------|
| 1-7 | 资产总计 ~ 固定资产 | 资产负债表查询结果的 `current_amount` |
| 8-14 | 负债合计 ~ 长期借款 | 同上 |
| 15-17 | 所有者权益 ~ 未分配利润 | 同上 |
| - | 资产负债率 | 计算值：负债合计 / 资产总计 × 100% |

#### 利润表关键科目

| # | 名称 | 数据来源 |
|---|------|---------|
| 1 | 营业收入 | 利润表查询结果，`item_standard` 精确匹配 |
| 2 | 营业成本 | 同上 |
| 3 | 管理费用 | 同上 |
| 4 | 销售费用 | 同上 |
| 5 | 财务费用 | 同上 |
| 6 | 研发费用 | 同上 |
| 7 | 营业利润 | 同上 |
| 8 | 利润总额 | 同上 |
| 9 | 净利润 | 同上 |
| - | 毛利润率 | 计算值：`(营业收入-营业成本) / 营业收入 × 100%` |
| - | 净利润率 | 计算值：`净利润 / 营业收入 × 100%` |
| - | 研发费用占比 | 计算值：`研发费用 / 营业收入 × 100%` |

---

## 六、配置项

```properties
# 外部表格提取状态接口地址（不含 doc_id，调用时拼接）
api.doc-table-extract-state-url=${api.base-url}/api/extract/open/doc/docTableExtractState

# Word 报告模板文件路径（支持 classpath: 前缀）
api.report-template-path=classpath:templates/sxd/报告模板.docx
```

| 属性 | 说明 |
|------|------|
| `api.doc-table-extract-state-url` | 表格提取状态接口基地址，调用时在末尾拼接 `/{doc_id}` |
| `api.report-template-path` | 报告模板路径。支持 `classpath:`（默认）和 `file:` 外部路径 |

> 其他共享配置（`api.doc-query-data-url`、`api.default-token` 等）参见[要素提取功能说明](要素提取功能说明.md#六配置项)。

---

## 七、错误码

| 错误码 | 触发条件 | HTTP 状态码 |
|--------|---------|------------|
| `TASK_NOT_FOUND` | `taskId` 在 `application_record` 表中不存在 | 400 |
| `DOC_NOT_FOUND` | `taskId` 在 `application_doc` 表中无关联的财务报表记录 | 400 |
| `TABLE_STATE_FAILED` | 外部表格提取状态接口调用失败（网络异常 / 非成功响应） | 400 |
| `DOC_QUERY_FAILED` | 外部提取数据查询接口（资产负债表或利润表）调用失败 | 400 |
| `NO_BALANCE_SHEET_DATA` | 所有文档均未查询到有效的资产负债表数据 | 400 |
| `TEMPLATE_NOT_FOUND` | 模板文件不存在（检查 `api.report-template-path` 路径） | 400 |
| `DOC_CREATE_FAILED` | Word 文档生成异常（POI 写入失败） | 400 |

---

## 八、与要素提取功能的关系

```
要素提取功能                     报告生成功能
─────────────────               ────────────────
GET /extract-status/{task_id}
  → completed=true
                                  GET /sxd/report/{task_id}
                                    → 查客户信息 → 查财务报表
                                    → docTableExtractState → 确定表类型
                                    → queryData(资产负债表) → 获取科目
                                    → queryData(利润表) → 获取科目
                                    → 加载模板 → 替换占位符 → 插入表格
                                    → 返回 .docx 下载

GET /export-data/business/{task_id}
GET /export-data/finance/{task_id}
（下载原始 xlsx）
```

> 报告生成功能依赖于要素提取完成（`completed=true`），复用同一套 `application_doc` 和 `queryData` 外部 API。不同之处在于：导出功能下载原始 xlsx 文件，报告功能查询结构化数据并聚合成 Word 文档。
